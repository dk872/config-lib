def fill_defaults(config, schema, path=""):
    # TODO: Рекурсивно заповнити конфігурацію типовими значеннями згідно зі схемою.
    return result


def mask_secrets(config: dict, secret_fields: list[str], mask: str = "***") -> dict:
    # TODO: Замаскувати секретні поля у конфігурації вказаним значенням.
    return result
